#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char const *argv[])
{
    if (argc != 3) {
        fprintf(stderr, "Usage: %s <nome_arquivo_entrada> <nome_arquivo_saida>\n", argv[0]);
        return EXIT_FAILURE;
    }

    

    return EXIT_SUCCESS;
}
